# Contoso Cart

A tiny shopping-cart module used to demo an autonomous coding agent
(GitHub Copilot SDK) that plans a change, edits the code, and opens a Pull Request.

## Run tests

```bash
npm install
npm test
```
